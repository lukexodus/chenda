/**
 * This file is intentionally minimal.
 * The canonical auth store lives in @/lib/store — use that instead.
 * This re-export exists only for backwards compatibility.
 */
export { useAuthStore } from "@/lib/store";
export type { User } from "@/lib/store";
